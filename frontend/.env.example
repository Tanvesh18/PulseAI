# Optional in local development; set to the deployed backend origin in production.
VITE_API_URL=
# Use the same Web application OAuth client ID as backend/.env.
VITE_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
