PORT=4000
FRONTEND_ORIGIN="http://localhost:3000"
OIDC_ISSUER="https://identity.example.com"
OIDC_AUDIENCE="pulse-ai-api"
OIDC_JWKS_URL="https://identity.example.com/.well-known/jwks.json"
ALLOW_DEV_AUTH=true
DEV_AUTH_SUBJECT="dev-employee-avery"
# Neon: copy both URLs from the project's Connect dialog.
# Keep the complete URL, including the TLS parameters.
# Pooled connection (hostname contains -pooler): used by the API.
DATABASE_URL=""
# Direct connection (connection pooling off): used by Prisma migrations.
DIRECT_URL=""
# Setup guide: docs/hosted-postgresql.md
